module first_module

import IO;

//text = ["andra", "moi", "ennepe", "Mousa", "polutropon"];

public list[str] find2(list[str] text){
  return 
    for(s <- text)
      if(/o/ := s)
        append s;        
}