module first_module

import String;

text = ["andra", "moi", "ennepe", "Mousa", "polutropon"];
public int count(list[str] text){
  n = 0;
  for(s <- text)
    if(/\w{0,}a\w{0,}/ := s)
      n +=1;
  return n;
  }